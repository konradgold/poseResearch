[project]
name = "poseResearch"
version = "0.1.0"
description = "Pose Estimation and Prediction Pipeline"
readme = "README.md"
requires-python = ">=3.8.20"
dependencies = [
    "easydict>=1.13",
    "human-body-prior>=0.8.5.0",
    "imageio>=2.35.1",
    "ipdb>=0.13.13",
    "openai>=1.83.0",
    "opencv-python>=4.11.0.86",
    "peft>=0.13.2",
    "prettytable>=3.11.0",
    "roma>=1.5.2.1",
    "scikit-learn>=1.3.2",
    "smplx>=0.1.28",
    "tensorboardx>=2.6.2.2",
    "torch>=2.5.1",
    "transformers>=4.46.3",
    "ultralytics>=8.3.146",
    "wandb>=0.21.0",
]

[build-system]
requires = ["setuptools>=80.9.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["MotionBERT"]

[tool.black]
line-length = 88
target-version = ['py312']
include = '\.pyi?$'
# Exclude all directories except the ones we want
exclude = '''
/(
    .github
  | .vscode
  | .venv
  | chatPose
  | configs
  | MaskAnyone
  | models
  | Motion-Agent
  | MotionBERT
  | pose_estimation
  | PoseGPT
)/
'''
skip-string-normalization = false

[tool.ruff]
line-length = 88
exclude = [
    ".git",
    ".venv",
    "build",
    "dist",
    "__pycache__",
    "*.egg-info",
    ".github",
    ".vscode",
    "chatPose",
    "configs",
    "MaskAnyone",
    "models",
    "Motion-Agent",
    "MotionBERT",
    "pose_estimation",
    "PoseGPT",
    "train_custom.py",
]

[tool.uv.workspace]
members = [
    "MotionBERT",
]
